# Author: Groovey
# Date: 2016-06-26 17:27:05

# Run the migration
UP: >

    CREATE TABLE `child` (
    `id` int(11) NOT NULL,
    `parent_id` int(11) NOT NULL,
    `name` varchar(200) COLLATE utf8_unicode_ci NOT NULL,
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci;

    ALTER TABLE `child` ADD KEY `id` (`id`);

    ALTER TABLE `child` CHANGE `id` `id` INT(11) NOT NULL AUTO_INCREMENT;


# Reverse the migration
DOWN: >

    DROP TABLE child;
